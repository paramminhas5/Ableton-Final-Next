// SEO metadata is handled by Next.js generateMetadata() at the page level.
// This stub exists for backward-compat with any legacy import.
export function SEO(_props: {
  title: string;
  description: string;
  keywords?: string;
  image?: string;
  url?: string;
  type?: "website" | "article" | "course";
}) {
  return null;
}
